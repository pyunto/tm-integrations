{
  "name": "tm-integrations",
  "version": "0.0.0",
  "private": true,
  "description": "Open-source client libraries for Pyunto Time Management: REST SDK and MCP server.",
  "license": "MIT",
  "workspaces": [
    "sdk",
    "mcp"
  ],
  "scripts": {
    "build": "npm run build -w pyunto-tm-sdk && npm run build -w pyunto-tm-mcp",
    "test": "npm run test -w pyunto-tm-sdk && npm run test -w pyunto-tm-mcp"
  },
  "engines": {
    "node": ">=18"
  }
}
